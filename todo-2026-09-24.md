# CrypDog 生产隐患整改清单（2026-09-24）

## 本轮已完成

- [x] 为活动支付意向增加数据库唯一 `allocation_key`，防止多实例并发撞单；启动时为旧数据回填并在发现冲突时阻止启动。
- [x] Webhook 重试改为带租约的任务抢占，进程在发送前崩溃时任务可自动重新领取。
- [x] TRON 扫描跟随 TronGrid fingerprint 分页，避免单页超过 50 笔时漏扫。
- [x] 就绪探针纳入扫描器健康状态，避免节点全部失效时仍接收订单。
- [x] 增加多实例分配、TRON 分页、Webhook 崩溃恢复、RPC 故障和数据库恢复测试。

## 仍需处理

- [ ] 生产环境 PostgreSQL 启用 TLS，避免使用 `sslmode=disable`。
- [ ] 生产 Compose 不直接暴露应用 HTTP 端口，只允许 HTTPS 反向代理访问。
- [ ] 轮换当前工作区配置中的 TronGrid/Alchemy API Key；日志和数据库中的 Webhook URL、响应体需要脱敏。
- [ ] 为限流器增加过期清理，或迁移到网关/Redis，避免攻击者制造大量 IP 键导致内存增长。
- [ ] 固定 Docker 基础镜像和 PostgreSQL 镜像 digest，systemd 使用非 root 用户。
- [ ] 增加多实例分配、TRON 分页、Webhook 崩溃恢复、RPC 故障和数据库恢复测试。
